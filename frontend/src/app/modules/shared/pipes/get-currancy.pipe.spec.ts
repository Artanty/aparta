import { GetCurrancyPipe } from './get-currancy.pipe';

describe('GetCurrancyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCurrancyPipe();
    expect(pipe).toBeTruthy();
  });
});
