import { HumanMonthPipe } from './human-month.pipe';

describe('HumanMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
